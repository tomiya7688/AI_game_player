# 画面キャプチャ

`WindowsScreenCapture` はWin32 APIで仮想デスクトップをBGRAフレームとして取得する。取得だけを責務とし、OCR・特徴量抽出・OS入力は行わない。Windows以外では明示的にエラーにする。

`ScreenFrame` は幅・高さ・生BGRAバイト列を持つ。後続の解析Machineはこの値を受け取り、`ScreenObservation`へ変換する。